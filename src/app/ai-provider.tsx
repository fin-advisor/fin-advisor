"use client";

// This file is no longer used due to the removal of the ai/rsc library.
// It is kept to avoid breaking imports if it were referenced elsewhere.
export function AIProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
