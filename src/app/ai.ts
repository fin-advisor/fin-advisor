"use server";
import { createAI } from "ai/rsc";
import { generate } from "./actions";

// This file is no longer used but kept to avoid breaking imports if referenced elsewhere.
// The new implementation uses a direct server action.
